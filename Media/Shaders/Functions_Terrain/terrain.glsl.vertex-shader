#version 420


layout (location = 0) in    vec3	vPos;
layout (location = 1) in    vec2	vTex;


layout (location = 0) out   vec2	tcTex;


void main()
{
	gl_Position = vec4(vPos, 1.0f);
    
    tcTex = vTex;
}
